using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BoxForThingsScripts : MonoBehaviour
{
    
    void Awake()
    {
        var stonesControl1Scr = gameObject.GetComponent<stonesControl1>();
        if (stonesControl1Scr = null)
        {
            // ищем другой скрипт
        }
        //stonesControl1Scr.
    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
