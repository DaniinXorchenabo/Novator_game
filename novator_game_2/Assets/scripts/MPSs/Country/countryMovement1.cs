using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class countryMovement1 : ParentCountryMovement
{
    // Start is called before the first frame update
    void Awake()
    {
        ParentAwake();
    }

    // Update is called once per frame
    void Update()
    {
        ParentUpdate();
    }
}
